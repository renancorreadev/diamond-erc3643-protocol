# Top holders de um token (ordenados por balance)
query TokenTopHolders($id: String!, $first: Int = 10) {
  token(id: $id) {
    id
    totalSupply
    holderCount
    holders(first: $first) {
      address
      balance
    }
  }
}

# Variáveis:
# { "id": "1", "first": 10 }
